# 2026-09-05

## Qué se hizo

- Se renombraron escenarios, epics, tests y corridas de `playground/` a `XXX-YYY-slug` (`XXX` el epic, `YYY` el orden del escenario dentro del epic). Se agregó `playground/README.md`, versionado a propósito, para que la convención sobreviva a que el resto del directorio esté ignorado.
- Se creó `corpus/referencia/referencia-pyme.md`, el par de `referencia-developer.md` en un dominio distinto: Andrea Bustos, dueña de una PYME de insumos educacionales, con dos semanas (10 al 23 de agosto de 2026) en las tres partes del formato (dictado, ground truth en formato bitácora, estado reconstruible).
- Se creó `corpus/README.md` con el índice de los dos corpus de referencia y para qué sirve cada uno.

## Decisiones

- Escenarios, tests y corridas se nombran `XXX-YYY-slug`, con el epic como `XXX`. Un escenario dice a qué epic pertenece sin abrirlo, y el orden alfabético es el orden real. El número no se renumera cuando cambia el orden de trabajo.
- El corpus de PYME se escribe con el vocabulario actual de `spec/` (`ambitos/`, marcas de la ontología cerrada, horizontes de `PENDIENTES.md`), no con el de `corpus/simulaciones/flujo-pyme-semana.md`, que es del diseño anterior (`entradas/`, `tareas/`, `entidades/`). Se tomaron de ahí las ideas de negocio, no la forma.
- El segundo corpus existe para probar algo que el primero no puede: **cadencias de fecha impuesta desde afuera** (SII, banco, año escolar) en vez de ritmo propio, y cadencias de evento y de ausencia que nacen de una frase de la usuaria y viven en el ámbito `clientes/`, no en cada cliente.

- Los horizontes de pendientes son vocabulario abierto (`### Horizontes` del libro de estilo), no de TUKU. Andrea usa `^esta-semana` y `^proxima-semana` donde el corpus de developer usa `^este-turno` y `^proximo-turno`: misma escalera, mismo janitor, distinto nombre. Escribir el segundo corpus fue lo que lo dejó a la vista.

- Se adoptó la **semana** como ciclo de partida en `template/vanilla/`: los horizontes pasaron de `^este-turno`/`^proximo-turno` a `^esta-semana`/`^proxima-semana` en `PENDIENTES.md` y en la tabla de `### Horizontes` del libro de estilo, y `ambitos/CADENCIAS.md` viene con dos cadencias de ciclo sembradas (cierre de la semana el domingo, fin de semana el viernes). `spec/pendientes.md` se alineó y ahora dice explícitamente qué parte de la escalera es de TUKU (`atrasados`, `sin-fecha`) y qué parte es del autor (los tres del medio).
- El fixture byte a byte de `001-001` se regeneró. El test detectó el cambio antes de regenerarlo, que es exactamente para lo que está.

## Queda pendiente

- Correr el escenario `001-001-instalacion-minima` con una persona real.
- Podar `docs/libro-de-estilo.md`: mover su §7 a `spec/`.
- Del corpus de PYME: `**aprendizaje**` aparece como clasificación abierta nueva y no está en el libro de estilo. Decidir si se formaliza o si el caso se cubre con `**decisión**`.
- `docs/libro-de-estilo.md` sigue con `^este-turno` y `^proximo-turno`. Es el libro de estilo del autor real, que sí trabaja por turnos, así que no está mal: queda anotado para revisarlo cuando se pode ese documento.
- Del corpus de developer, sin tocar desde la revisión de seguridad: el teléfono real en la línea de bioimpedanciometría, y los nombres `Johan` y `Takao`.
