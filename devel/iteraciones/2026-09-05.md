# 2026-09-05

## Qué se hizo

- Se renombraron escenarios, epics, tests y corridas de `playground/` a `XXX-YYY-slug` (`XXX` el epic, `YYY` el orden del escenario dentro del epic). Se agregó `playground/README.md`, versionado a propósito, para que la convención sobreviva a que el resto del directorio esté ignorado.
- Se creó `corpus/referencia/referencia-pyme.md`, el par de `referencia-faena.md` en un dominio distinto: Andrea Bustos, dueña de una PYME de insumos educacionales, con dos semanas (10 al 23 de agosto de 2026) en las tres partes del formato (dictado, ground truth en formato bitácora, estado reconstruible).
- Se creó `corpus/README.md` con el índice de los dos corpus de referencia y para qué sirve cada uno.
- Se adoptó la **semana** como ciclo de partida en `template/vanilla/`: los horizontes pasaron de `^este-turno`/`^proximo-turno` a `^esta-semana`/`^proxima-semana` en `PENDIENTES.md` y en la tabla de `### Horizontes` del libro de estilo, y `ambitos/CADENCIAS.md` viene con dos cadencias de ciclo sembradas (cierre de la semana el domingo, fin de semana el viernes). `spec/pendientes.md` se alineó. Se regeneró el fixture byte a byte de `001-001`, que el test detectó roto antes de regenerarlo.
- Se renombró `corpus/simulaciones/flujo-turno.md` a `flujo-faena.md` y `corpus/referencia/referencia-developer.md` a `referencia-faena.md`. El diario del 2026-09-04 conserva los nombres viejos: es historia.
- Se borró `devel/technical_stack.md`, copia byte a byte de `devel/VAULT/technical_stack.md` que describía el diseño anterior. Sus tres líneas rescatables (Obsidian, Quartz, Telegram) pasaron a `devel/epics.md` como material de epics siguientes.
- Se podó `devel/entorno-devel.md` de 42 a 30 líneas: quedaron los comandos y las tres invariantes de determinismo (TZ=UTC, fecha inyectada por parámetro, round-trip byte a byte).

## Decisiones

- El corpus de PYME existe para probar algo que el de faena no puede: **cadencias de fecha impuesta desde afuera** (SII, banco, año escolar), y cadencias de evento y de ausencia que viven en el ámbito `clientes/`, no en cada cliente.
- Los horizontes de pendientes son vocabulario **abierto** del autor (`### Horizontes` del libro de estilo), no de TUKU. El estado cero los siembra en semanas porque es el ritmo de casi todo el mundo; un autor por turnos los renombra.
- Los corpus se nombran por el contexto que retratan, no por el oficio de quien los escribe (`referencia-faena`, no `referencia-developer`).

## Queda pendiente

- Correr el escenario `001-001-instalacion-minima` con una persona real.
- Podar `docs/libro-de-estilo.md`: mover su §7 a `spec/`.
- Del corpus de PYME: `**aprendizaje**` aparece como clasificación abierta nueva y no está en el libro de estilo. Decidir si se formaliza o si el caso se cubre con `**decisión**`.
- `docs/libro-de-estilo.md` sigue con `^este-turno` y `^proximo-turno`. Es el libro de estilo del autor real, que sí trabaja por turnos, así que no está mal: queda anotado para revisarlo cuando se pode ese documento.
- Del corpus de faena, sin tocar desde la revisión de seguridad: el teléfono real en la línea de bioimpedanciometría, y los nombres `Johan` y `Takao`.

## Adenda

- Se revisaron los diez `README.md` del repo (fuera de `devel/VAULT/`) para bajar AI-smell y extensión. Reescritos a fondo: `README.md` raíz (76→29 líneas: fuera los badges de shields.io, la frase duplicada de `docs/brief.md`, la lista de estructura que ya duplicaba `DEVEL.md`), `docs/README.md` (20→10, la tabla ya decía todo lo que la prosa repetía debajo) y `tests/README.md` (95→55, corregido el enlace roto a `devel/plan-implementacion.md`, que no existe). Los demás (`corpus/`, `corpus/escenarios/`, `template/`, `playground/`, `tests/escenarios/`, `tests/scripts/`, `devel/iteraciones/`) ya estaban en la prosa breve del resto del proyecto y se dejaron igual.
