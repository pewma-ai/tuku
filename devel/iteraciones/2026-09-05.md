# 2026-09-05

## Qué se hizo

- Se renombraron escenarios, epics, tests y corridas de `playground/` a `XXX-YYY-slug` (`XXX` el epic, `YYY` el orden del escenario dentro del epic). Se agregó `playground/README.md`, versionado a propósito, para que la convención sobreviva a que el resto del directorio esté ignorado.
- Se creó `corpus/referencia/referencia-pyme.md`, el par de `referencia-faena.md` en un dominio distinto: Andrea Bustos, dueña de una PYME de insumos educacionales, con dos semanas (10 al 23 de agosto de 2026) en las tres partes del formato (dictado, ground truth en formato bitácora, estado reconstruible).
- Se creó `corpus/README.md` con el índice de los dos corpus de referencia y para qué sirve cada uno.
- Se adoptó la **semana** como ciclo de partida en `template/vanilla/`: los horizontes pasaron de `^este-turno`/`^proximo-turno` a `^esta-semana`/`^proxima-semana` en `PENDIENTES.md` y en la tabla de `### Horizontes` del libro de estilo, y `ambitos/CADENCIAS.md` viene con dos cadencias de ciclo sembradas (cierre de la semana el domingo, fin de semana el viernes). `spec/pendientes.md` se alineó. Se regeneró el fixture byte a byte de `001-001`, que el test detectó roto antes de regenerarlo.
- Se renombró `corpus/simulaciones/flujo-turno.md` a `flujo-faena.md` y `corpus/referencia/referencia-developer.md` a `referencia-faena.md`. El diario del 2026-09-04 conserva los nombres viejos: es historia.
- Se borró `devel/technical_stack.md`, copia byte a byte de `devel/VAULT/technical_stack.md` que describía el diseño anterior. Sus tres líneas rescatables (Obsidian, Quartz, Telegram) pasaron a `devel/epics.md` como material de epics siguientes.
- Se podó `devel/entorno-devel.md` de 42 a 30 líneas: quedaron los comandos y las tres invariantes de determinismo (TZ=UTC, fecha inyectada por parámetro, round-trip byte a byte).
- Se movieron los escenarios de `corpus/escenarios/` a `tests/escenarios/`: un Dado/Cuando/Entonces es una aserción, no un dato, y vivir separado del arnés solo producía punteros cruzados entre los dos árboles. Se implementó el arnés de `001-002-instalacion-local`, hasta ahora sin test. El fixture de `001-001` bajó de once archivos a uno (`AHORA.md`, lo único que `instalar()` transforma, luego aplanado quitando el nivel `esperado/` que ya no separaba nada): el resto se compara en vivo contra `template/vanilla/`.
- Se agregó `tests/correr.sh` para filtrar por epic o escenario y luego se quitó: el nombre del test (`test_XXX_YYY_slug`) ya es el tag, y `pytest -k` lo filtra sin necesitar un script propio.
- Se borró entera la suite antigua de `tests/` (conftest, specref, veinte `test_*.py` de importaciones a un paquete `tuku` que ya no existe): `tests/` se construye solo desde los epics, no por adelantado.
- Se agregó el escenario `001-003-destino-no-vacio`: `install.sh` pregunta antes de sobrescribir un destino no vacío, y no sobrescribe si no se confirma. Es el primer test del epic 001 que prueba `install.sh` mismo (no `instalar()`). El caso de no confirmar se prueba con un subproceso sin terminal de control (`start_new_session=True`): no puede abrir `/dev/tty` y cancela solo. El caso de confirmar necesitaba escribirle una respuesta a un prompt que lee `/dev/tty`, no la entrada estándar: se agregó `pexpect` como dependencia de dev (`pyproject.toml`), que abre una pty real; el test no espera a que la descarga termine, solo ve que el script sigue de largo tras el "s" y mata el proceso ahí. Se sumó el caso de `TUKU_FORCE=1` (salta la pregunta entera). Al escribirlo apareció un cuelgue real: matar solo el proceso principal no bastaba, porque para esa altura ya lanzó `curl | tar` como su propia tubería, y una señal al shell a veces se difiere hasta que el comando en curso termina. Se corrigió matando el grupo de procesos entero (`os.killpg`), y se aplicó el mismo arreglo al caso de confirmar, que corría el mismo riesgo sin haberlo mostrado todavía.

## Decisiones

- El corpus de PYME existe para probar algo que el de faena no puede: **cadencias de fecha impuesta desde afuera** (SII, banco, año escolar), y cadencias de evento y de ausencia que viven en el ámbito `clientes/`, no en cada cliente.
- Los horizontes de pendientes son vocabulario **abierto** del autor (`### Horizontes` del libro de estilo), no de TUKU. El estado cero los siembra en semanas porque es el ritmo de casi todo el mundo; un autor por turnos los renombra.
- Los corpus se nombran por el contexto que retratan, no por el oficio de quien los escribe (`referencia-faena`, no `referencia-developer`).
- `test_001_001` y `test_001_002` verifican hoy lo mismo (ambos llaman `instalar()` directo, ninguno pasa por `install.sh`): la diferencia que promete el escenario 001-002 (camino `curl` vs. camino local) no está probada todavía. Se deja así a propósito, no se arregla de paso.

## Queda pendiente

- Correr el escenario `001-001-instalacion-minima` con una persona real.
- Podar `docs/libro-de-estilo.md`: mover su §7 a `spec/`.
- Del corpus de PYME: `**aprendizaje**` aparece como clasificación abierta nueva y no está en el libro de estilo. Decidir si se formaliza o si el caso se cubre con `**decisión**`.
- `docs/libro-de-estilo.md` sigue con `^este-turno` y `^proximo-turno`. Es el libro de estilo del autor real, que sí trabaja por turnos, así que no está mal: queda anotado para revisarlo cuando se pode ese documento.
- Del corpus de faena, sin tocar desde la revisión de seguridad: el teléfono real en la línea de bioimpedanciometría, y los nombres `Johan` y `Takao`.
- Probar de verdad la diferencia entre 001-001 y 001-002 (invocar `install.sh` sin red real, comparar contra `instalar()` directo). Hoy ninguno de los dos tests lo hace.
- Instalar `pytest` en el entorno del autor (fuera de `uv run`), para poder correr la suite sin pasar por `uv` cada vez.

## Adenda

- Se revisaron los diez `README.md` del repo (fuera de `devel/VAULT/`) para bajar AI-smell y extensión. Reescritos a fondo: `README.md` raíz (76→29 líneas: fuera los badges de shields.io, la frase duplicada de `docs/brief.md`, la lista de estructura que ya duplicaba `DEVEL.md`), `docs/README.md` (20→10, la tabla ya decía todo lo que la prosa repetía debajo) y `tests/README.md` (95→55, corregido el enlace roto a `devel/plan-implementacion.md`, que no existe). Los demás (`corpus/`, `corpus/escenarios/`, `template/`, `playground/`, `tests/escenarios/`, `tests/scripts/`, `devel/iteraciones/`) ya estaban en la prosa breve del resto del proyecto y se dejaron igual.

## Adenda · sesión de tarde: marco documental, capacidad y el fixture

**Los tres documentos del marco declaran qué son.** `docs/brief.md`, `docs/principios.md` y `spec/README.md` abren diciendo qué responde cada uno y qué no le toca. El criterio que los separa: si un `git mv` vuelve falso un párrafo, es spec; si sirve para decidir algo que nadie escribió todavía, es principio; si le habla a quien aún no decide usar TUKU, es brief.

**`docs/principios.md` quedó sin un solo nombre de archivo** (111→81 líneas). Lo que salió estaba duplicado literal en `spec/`: la tabla de primitivas con paths y el árbol (en `spec/README.md`), los tres roles y la convención de mayúsculas (`spec/ambitos.md`), las notas tipadas (`spec/notas.md`). El único contenido con destino nuevo fue el estado cero, que pasó a `template/README.md`. La numeración 1 a 9 se conserva porque hay veinte referencias por número en `spec/` y `devel/`. §6 pasó de dos ejes a tres; §8 cambió de "notas espontáneas o tipadas" (que era spec) a la regla ética, que era lo único normativo que tenía.

**Los tres ejes entraron al brief.** Cada hecho queda indexado por cuándo ocurrió, sobre qué y qué se concluyó de él. La tesis no estaba escrita en ninguna parte del repositorio, solo implícita en la deliberación de agosto. El valor está en los cruces, no en cada eje por separado, y de ahí salen las primitivas en ese orden.

**Enlaces navegables desde GitHub.** Las referencias entre documentos pasaron de backtick a enlace Markdown real en once archivos. Los `[[wikilink]]` que quedan son contenido del producto (lo que el autor escribe en su vault), no navegación, y se dejaron. Encontrado de paso: `spec/README.md` citaba "el contrato en `../devel/README.md`", archivo que no existe y del que no hay rastro en ningún otro documento. Marcado con `#REVISAR`, no inventado.

## Decisiones de la tarde

- **`CAPACIDAD.md` es el tercer archivo por directorio**, y el único opcional. Se acumula en vez de prevalecer, porque es una cantidad y no una regla: que `trabajo/` declare un costo no anula el de `trabajo/turnos/`, los dos consumen del mismo día. Es la única pieza del árbol que se lee sumando. Formato y cálculo en `spec/ciclo.md`, ubicación en `spec/ambitos.md`. Resuelve la decisión abierta de `spec/README.md` sobre dónde vive la capacidad del ciclo.
- **La capacidad no se declara en horas.** Vocabulario cerrado de cuatro escalones (`el día entero`, `casi todo el día`, `media jornada`, `un rato`), como la ontología cerrada de la bitácora, y el linter normaliza las variantes. Pedirle al autor que cuantifique su semana es la forma más rápida de que deje de mantener el archivo, y además es precisión falsa.
- **Nada de lo que sirve para anticipar puede ser requisito para registrar.** Cadencias y capacidad son deseables, no puertas de entrada: sin ellas el sistema registra y devuelve lo registrado, con ellas además se adelanta. Quedó como segundo corolario del principio 2, que es lo que explica por qué el estado cero es tan chico.
- **El estado cero siembra `CADENCIAS.md` y `CAPACIDAD.md` de `personal/` sin un solo dato**, diciendo que todavía no se ha declarado nada y por qué conviene. Un archivo que se explica solo es la forma barata de que el autor sepa que puede escribirlo, y no rompe el principio 2 mientras no le pida declarar nada para empezar.
- **Se eliminó el fixture de `001-001`.** Congelaba `AHORA.md` entero, incluidos el frontmatter fijo, el título y las líneas en blanco, que `instalar()` no toca. Cualquier cambio en el template obligaba a regenerarlo a mano, que es el defecto que `tests/escenarios/README.md` ya declaraba querer evitar. Ahora el esperado se deriva del template real y lo único escrito a mano son los siete encabezados de día, que es donde estuvo el bug.
- **Tercera afirmación en los tests de instalación:** ningún archivo del vault conserva un placeholder fuera de un bloque de código. Es la que cubre el crecimiento del template: un placeholder nuevo que el instalador no sabe sustituir pasaba en silencio. Encontró uno al escribirse (`ambitos/CADENCIAS.md`, dentro de su ejemplo de formato), lo que obligó a declarar la regla: dentro de un fence es documentación, fuera es una marca pendiente.
- **`tests/scripts/` se llenó.** La condición que el propio README ponía era un segundo consumidor real, y ya existía: `001-001` y `001-002` duplicaban el cuerpo entero. `vault.py` tiene los tres pasos compartidos.
- **Fuera los conteos de archivos del estado cero.** "Once archivos" en `template/README.md` y "11 archivos" en `epics.md` envejecían con cada cambio del template, el mismo defecto del fixture.

## Queda pendiente de la tarde

- `spec/ambitos.md`, `#REVISAR`: el reparto de capacidad (bruto en `personal/`, costos fijos acumulables en el resto) es propuesta, no decisión tomada. La alternativa es que cada ámbito declare su capacidad asignada y el total sea la suma.
- `spec/README.md`, `#REVISAR`: qué se hace con la referencia al contrato en `../devel/README.md`, que no existe. Escribirlo o borrar la frase.
- `docs/brief.md`, `#REVISAR`: si la capacidad merece decirse en el brief o basta con que esté en las specs. No es un cuarto eje: cuelga del ámbito y se gasta en el tiempo, así que mide cuánto cabe en el cruce de los dos primeros.
- `docs/brief.md`, `#REVISAR`: reubicar la lista de herramientas concretas (Claude Code, Hermes, Antigravity, Obsidian, Telegram) que salió del brief. El repositorio no las nombra en ningún otro lugar. Destino natural: `devel/entorno-devel.md` o `spec/agente.md`.

## Adenda · reestructuración de epics

**Los epics cortan por estado del vault, no por primitiva.** Las fases de `que_implementar.md` cortan por primitiva (entrada, pendientes, ámbitos, cadencias, notas) y los epics por el estado inicial con el que empiezan. Son ejes perpendiculares, y el corte por estado ya estaba latente en la columna Fixture de la tabla de fases: `vacio`, `primer-dia`, `ciclo-en-curso`, `ciclo-por-cerrar`, `historico` agrupan casi exactamente como quedaron los cinco epics. El motivo del cambio es el criterio de entrega: "los pendientes funcionan" no es una experiencia, "el día uno funciona" sí.

Consecuencia: los epics 002 y 003 construyen las mismas capacidades dos veces, contra vaults distintos. En el 002 el vault está vacío y cada capacidad crea lo que necesita; en el 003 ya hay árbol, notas y pendientes con historia, y la misma capacidad tiene que aprovecharlos.

**`que_implementar.md` es la vista de alto nivel de `epics.md`**, no un documento paralelo. Quedó declarado en su cabecera, con la regla de precedencia: si los dos se contradicen, manda `epics.md`.

## Decisiones

- **Cinco epics.** 001 instalable (`vacio`), 002 el día uno (`primer-dia`), 003 el día ciento cincuenta (`ciclo-en-curso`), 004 abrir y cerrar el ciclo (`ciclo-por-cerrar`), 005 que note lo que nadie pidió (`historico`).
- **Las cadencias se declaran en el 002 y se cosechan en el 004**, porque emiten en la apertura del ciclo: una cadencia declarada el día uno no produce nada hasta que pasa el tiempo.
- **El endurecimiento (fase 8) pasa a la wishlist.** No agrega capacidades y nada lo bloquea. La numeración de fases salta la 8 en vez de renumerar, porque hay referencias por número repartidas por el repositorio. Lo que vale desde ya, sin la fase: un error del autor nunca se rechaza, se reporta.
- **Escribir en un día actual o futuro fecha el pendiente** y lo transcluye al inicio de ese día. Agendar es escribir donde corresponde, no un comando aparte. Ataja la escalera de horizontes: un pendiente puede nacer con fecha exacta sin pasar por `^sin-fecha`. Escrito en `spec/pendientes.md`.
- **El enlazado retroactivo llega hasta `AHORA.md` y no más.** Los ciclos cerrados no se reescriben para agregarles enlaces que no tenían. La distinción es reparar (al archivar, sí, para no romper el principio 1) contra enriquecer el pasado (nunca solo, es operación deliberada del autor). Escrito en `spec/ambitos.md`.
- **El material sale de `mac-jpgil`, epic por epic.** Las prácticas probadas del vault real (sus `AGENTS.md`, templates, procesos y reglas) son la fuente de lo que se implementa, pero se traen cuando el epic las necesita. Traerlas por adelantado es construir sobreingeniería sobre prácticas que quizá no sobreviven al empaquetado.
- **El epic 002 hace la versión mínima de las fases 3 y 5**, y el 003 las completa. Sin ese límite escrito, el epic 002 no cierra nunca: crear un ámbito sí, resolver reglas por cercanía no; crear una nota y enlazarla sí, notas tipadas y destilado no.

## Queda pendiente

- **La consecuencia "nota" no existe en `spec/flujo-informacion.md`.** Su tabla tiene pendientes, enlaces, cadencias y propuesta. El epic 002 la exige (un dictado produce la nota, la entrada de bitácora y opcionalmente el enlace al ámbito). Sale de la práctica de `mac-jpgil`. Se agrega cuando empiece el 002, no antes.
- ~~"Ver además" no está en `spec/notas.md`, solo en la matriz de `docs/libro-de-estilo.md`.~~ Resuelto en la poda de esa misma tarde: ver adenda siguiente.
- La sección "Orden de las pruebas" de `que_implementar.md` sigue con el detalle por área (janitors, aserciones). Bajo la relación nueva ese detalle pertenece a los epics. No se movió: es un refactor grande y toca hacerlo epic por epic, cuando cada uno lo consuma.

## Adenda · poda de `docs/libro-de-estilo.md`

**Se cerró la última decisión abierta del epic 001.** El documento se comparó sección por sección contra `spec/`: ocho de sus nueve secciones estaban duplicadas casi literal (§1 en `flujo-informacion.md`, §2 repartido entre `ciclo.md`, `pendientes.md`, `cadencias.md`, `ambitos.md`, `notas.md`, §3 en `bitacora.md`, §4 ya vivía de verdad en `template/vanilla/LIBRO-DE-ESTILO.md`, §5 y §6 en `ciclo.md`). La única sección sin duplicar era §7, la matriz de reglas y responsabilidades.

**Bug encontrado al comparar: `spec/bitacora.md` citaba el documento equivocado.** Decía que los vocabularios abiertos "viven en `docs/libro-de-estilo.md`", pero ningún janitor en producción lee ese archivo: es documentación de diseño del repositorio de software, no el archivo que se instala en el vault de un autor. Lo que los janitors leen de verdad es `LIBRO-DE-ESTILO.md`, sembrado por `template/vanilla/`. La confusión venía de que ambos archivos tienen casi el mismo contenido en sus tres tablas, y se coló en la sesión que convirtió esa referencia a enlace real. Corregido: ahora apunta al concepto del vault, con `template/vanilla/LIBRO-DE-ESTILO.md` como semilla concreta.

**Tres filas de la matriz no estaban cubiertas y se migraron antes de borrar:**
- "Ver además" con motivo tras cada enlace, y su calidad, pasaron a `spec/notas.md`. Cierra el bloqueante que el epic 003 tenía pendiente.
- El emparejamiento no literal al cerrar un pendiente (`~~(Hecho)~~` que no repite el texto exacto) pasó a `spec/agente.md`, como excepción al paso 5 determinista: ahí es juicio del agente, no lectura mecánica, y se confirma con el autor ante la duda.

**`docs/libro-de-estilo.md` se borró completo**, no se dejó reducido. `docs/README.md` y `devel/epics.md` actualizados para no citarlo.

## Decisiones

- `docs/libro-de-estilo.md` no existe más. Actualiza el criterio de salida del epic 001 (ya no incluye podarlo) y el bloqueante del epic 003 (ya resuelto).
- La referencia a "dónde viven los vocabularios abiertos" en `spec/` apunta al `LIBRO-DE-ESTILO.md` del vault del autor, nunca a un documento del repositorio de TUKU.
