# 2026-09-04

## Qué se hizo

- Se separó el contenido normativo que estaba mezclado en `devel/que_implementar.md` (10 fases + toda la spec del sistema) hacia `spec/`, un archivo por primitiva. `que_implementar.md` quedó reducido al plan de fases.
- Se creó `docs/glosario.md`.
- Se verificó ítem por ítem el apéndice "cambios pendientes en `docs/`" de `que_implementar.md`: 20 de 21 ya estaban resueltos en `docs/` y `spec/`. Se eliminó el apéndice; el único ítem vivo (qué declara `reglas/config.tuku.md`) pasó a decisiones abiertas de `spec/README.md`.
- Se creó `devel/epics.md`. Se corrigió para dejar explícito que `spec/` y `docs/` son provisionales y los epics los mueven, no al revés.
- Se creó `template/vanilla/`: el estado cero instalable, 11 archivos.
- Se creó `src/install_test_scenario.py`: mecanismo de instalación (copia la variante y resuelve fechas de `AHORA.md`). Resucita `src/` en la raíz, que es donde `pyproject.toml` ya apuntaba. Reemplaza como base al único código de instalación existente (`devel/VAULT/src/tuku/core/init.py`), que resultó ser del diseño anterior (`entradas/`, `tareas/`, `entidades/`) y no sirve.
- Primer intento: el mecanismo y el caso de prueba quedaron mal repartidos (`template/instalar.py`, `devel/escenarios/`). Se corrigió: `template/` es solo Markdown, el mecanismo va en `src/`, los casos narrativos van en `corpus/escenarios/` (dato, se acepta que haya cientos), y el arnés que eventualmente los ejecuta en `tests/escenarios/` y `tests/scripts/`.
- Se creó `devel/iteraciones/` (este archivo).
- Se resolvió qué hacer con `devel/VAULT/`: queda como historia, no como base para código nuevo. Lo que se rescate es puntual, decidido dentro del epic que lo necesite, nunca migración de bloque.
- Se corrió el primer escenario (`instalacion-minima`), primera vez: falló porque el mecanismo estaba invocado desde código no commiteado. Se hizo el commit `66f0711` y `git push` a `origin/devel`.
- Se creó `install.sh`: instalador de una línea vía `curl` (baja el tarball del branch, extrae `template/vanilla/`, corre `install_test_scenario.py`). Reemplaza a `git clone` como forma de instalar. Probado real contra GitHub, sin fallas: `curl -fsSL https://raw.githubusercontent.com/pewma-ai/tuku/devel/install.sh | sh -s -- <destino>`.

## Decisiones

- El instalador de TUKU es un template que se copia, no un CLI que se ejecuta primero. `template/` vive en la raíz, una carpeta por variante, sin composición entre variantes.
- Las specs son normativas dentro de un epic, y provisionales entre epics: el resultado de experimentar las corrige.
- Los tests de TUKU son narrativos (estilo Gherkin), no unitarios clásicos, porque buena parte del sistema depende de un agente. El caso vive en `corpus/escenarios/` (dato); el arnés que lo ejecuta, en `tests/escenarios/` y `tests/scripts/`; las corridas son desechables (`playground/epic-<n>_test-<x>/`, ignorado por git, se pisa al recrear el escenario).
- `reglas/config.tuku.md` declara zona horaria y tipo de ciclo. Cierra una de las tres decisiones abiertas de `spec/README.md`, a falta de tu visto bueno.
- El instalador para el autor final es un `curl` de una línea, no `npm`/`pipx`/`git clone`: lo que se instala es Markdown, no un paquete de un runtime, y eso contradice el principio 1.

## Queda pendiente

- Correr el escenario `instalacion-minima` con una persona real, no solo con `curl` en la terminal.
- Decidir si el estado cero se fija a una fecha (`--desde`) para volver a ser reproducible byte a byte, o si el criterio de salida de la fase 0 se reescribe.
- Podar `docs/libro-de-estilo.md`: mover su §7 (matriz de reglas y responsabilidades) a `spec/`, es lo único que falta antes de dejarlo como documento de diseño y no como starter duplicado.
- ~~`install.sh` no valida qué pasa si `<destino>` ya tiene contenido...~~ **Resuelto:** `install.sh` pregunta antes de sobrescribir si el destino no está vacío (lee de `/dev/tty`, ya que stdin viene ocupado por el pipe de `curl`); `TUKU_FORCE=1` lo salta para uso automatizado. `src/install_test_scenario.py` sigue sobrescribiendo sin preguntar, a propósito: es la herramienta de prueba para `playground/`, no la del usuario final. Probados los tres caminos (vacío, con contenido sin terminal → cancela limpio, `TUKU_FORCE=1` → sobrescribe).
- Al probar el arreglo salió un bug real: en un entorno sin `/dev/tty` interactivo, `[ -r /dev/tty ]` daba verdadero pero el `read` fallaba igual, y el script abortaba con un error crudo del shell en vez de cancelar limpio. Corregido envolviendo el `read` en un subshell que degrada a "cancelado" si `/dev/tty` no sirve.

Todo lo de hoy quedó commiteado (`66f0711`, `c37a8cb`) y empujado a `origin/devel`.
