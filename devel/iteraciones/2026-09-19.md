# 2026-09-19

## Qué se hizo

### Cierre formal del Epic 001 (Un TUKU mínimo instalable)

- **Verificación automatizada con red:** Se ejecutó [`tests/escenarios/001-01-instalacion-con-uv-tool.md`](../../tests/escenarios/001-01-instalacion-con-uv-tool.md) con el marcador `red` (`uv run pytest tests/escenarios/ -k 001_01 -m red`), confirmando que `uv tool install --force git+...@devel`, `pipx install` y la instalación local compilan el wheel, aíslan los binarios y siembran offline desde la copia empaquetada.
- **Aceptación humana completada:** Se validó el criterio de salida establecido en [`tests/escenarios/001-00-un-tuku-minimo-instalable.md`](../../tests/escenarios/001-00-un-tuku-minimo-instalable.md):
  - Instalación limpia y binario disponible en `PATH`.
  - Siembra de vault en directorio vacío vía `tuku init` sin conexión.
  - Rechazo limpio ante directorio con contenido previo.
  - Redacción manual de una primera entrada en `AHORA.md` guiada exclusivamente por [`AGENTS.md`](../../template/vanilla/AGENTS.md).
- **Actualización de gobernanza:** Se registró el estado `cerrado` en [`devel/epics.md`](../epics.md) y se marcó el pendiente resuelto en [`TODO.md`](../../TODO.md).

### Formalización de archivos derivados (`type: Derived`) y advertencia de edición

- **Tipificación OKF:** Se incorporó `Derived` en [`template/vanilla/reglas/types.md`](../../template/vanilla/reglas/types.md) para identificar archivos Markdown generados automáticamente a partir de fuentes canónicas.
- **Frontmatter y callout en `PENDIENTES-AMBITOS.md`:** Se actualizó [`template/vanilla/ambitos/PENDIENTES-AMBITOS.md`](../../template/vanilla/ambitos/PENDIENTES-AMBITOS.md) y la constante `_ENCABEZADO_AMBITOS` en [`src/tuku/propagate.py`](../../src/tuku/propagate.py) para declarar `type: Derived`, `origin: PENDIENTES.md`, `command: tuku todo propagate` y un callout `[!warning] Vista derivada` alertando que las ediciones se realizan en `PENDIENTES.md`.
- **Sincronización:** Se ejecutó [`src/tuku/_pack.py`](../../src/tuku/_pack.py) para replicar los cambios en el árbol empaquetado `_home/`.

### Unificación de `type: Todo` y mapa de dependencias

- **Unificación de vocabulario OKF:** Se reemplazó el tipo histórico `Pending` por `Todo` en [`template/vanilla/PENDIENTES.md`](../../template/vanilla/PENDIENTES.md), [`template/vanilla/reglas/types.md`](../../template/vanilla/reglas/types.md), [`src/tuku/doctor.py`](../../src/tuku/doctor.py) y [`spec/pendientes.md`](../../spec/pendientes.md). Con esto, el `type` de frontmatter coincide exactamente con el comando CLI (`tuku todo`), el módulo Python (`todo.py`) y el callout nativo de Obsidian (`[!todo]`).
- **Mapa de cableado estructural:** Se creó [`devel/dependencies-tree.md`](../dependencies-tree.md) conectando cada artefacto del vault con su plantilla, especificación normativa, módulos de producción y flujo de datos canónico/derivado.

## Decisiones

- **Epic 001 queda cerrado definitivamente:** La fase 0 (El vault que se puede abrir) satisface todos sus requisitos normativos y automatizados.
- **Contrato de vistas derivadas:** Todo Markdown generado automáticamente declara `type: Derived` con `origin` y `command` en su frontmatter, preservando la validez del contrato OKF ante `tuku doctor`.
- **Alineación de tipos con nouns del CLI:** El tipo OKF para pendientes es `Todo` (sustituyendo a `Pending`), manteniendo la regla de paridad uno a uno entre el sustantivo del CLI y el tipo normativo.

## Estado de verificación

- **Suite determinista:** 250/250 PASSED (5.8s).
- **Tests unitarios:** 169/169 PASSED (0.24s).
- **Escenarios de red (`001-01`):** 3/3 PASSED con socket e instalación real de `uv` y `pipx`.
- **Tipado y linters:** `ruff check .` y `mypy src tests` 100% limpios (76 archivos verificados).

## Queda pendiente

- Cierre del Epic 002 tras confirmación de revisión visual de `playground/002-09-crear-nota/`.
- Ejecución de [`tests/escenarios/003-08-el-mismo-vault-otro-arnes.md`](../../tests/escenarios/003-08-el-mismo-vault-otro-arnes.md) y revisión manual de turnos para cerrar el Epic 003.
