# tests/scripts

Pasos deterministas reutilizables entre escenarios: instalar una variante, invocar un janitor con argumentos (la "segunda vía" de `spec/flujo-informacion.md`), o construir un estado intermedio sin repetir la lógica en cada test de `tests/escenarios/`.

Vacío por ahora, a propósito: no hay janitors que invocar todavía (epic 1 no entra ninguno). Este directorio existe para cuando el epic 2 empiece a necesitar pasos compartidos, y así no se decide su forma antes de que haya un segundo consumidor real.
